/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.16 fireplanet2.glb 
*/

import React, { useRef } from 'react'
import { useGLTF } from '@react-three/drei'
import { useFrame } from "@react-three/fiber";

export default function FirePlanet2(props) {
  const groupRef = useRef()
  const { nodes, materials } = useGLTF('/fireplanet2.glb')

  useFrame(({ clock }) => {
    // Rotate the mesh around the Y axis
    const elapsedTime = clock.getElapsedTime();
    //orbit the location [0,0,0]
    groupRef.current.rotation.y += 0.02;
  });

  return (
    <group {...props} ref={groupRef} position={[-2, -2, 0]} dispose={null}>
      <mesh geometry={nodes.Planet.geometry} material={materials.Lava} />
      <mesh geometry={nodes.Atmosphere.geometry} material={materials.Clouds} scale={1.036} />
    </group>
  )
}

useGLTF.preload('/fireplanet2.glb')
